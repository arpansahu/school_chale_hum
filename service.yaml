apiVersion: v1
kind: Service
metadata:
  name: school-chale-hum-service
spec:
  selector:
    app: school-chale-hum
  ports:
    - protocol: TCP
      port: 8002
      targetPort: 8002
      nodePort: 32002
  type: NodePort